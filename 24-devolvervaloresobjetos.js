const productos = {
    precio: 30,
    disponible: true
}

console.log(Object.keys(productos));//nos devolvera un arreglo con los keys del objeto
console.log(Object.values(productos));// nos devolvera un arreglo con los valores del objeto
console.log(Object.entries(productos)); // nos va a retonar una matriz de llaves y valores
